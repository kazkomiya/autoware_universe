{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Parameters for Image Transport Decompressor",
  "type": "object",
  "definitions": {
    "autoware_image_transport_decompressor": {
      "type": "object",
      "properties": {
        "encoding": {
          "type": "string",
          "description": "The image encoding to use for the decompressed image. `rgb8` and `bgr8` reduce every image to 8 bits with three channels in that order. `default` keeps the bit depth, the channel count and the alpha channel of the compressed image.",
          "enum": ["default", "rgb8", "bgr8"],
          "default": "default"
        }
      },
      "required": ["encoding"]
    }
  },
  "properties": {
    "/**": {
      "type": "object",
      "properties": {
        "ros__parameters": {
          "$ref": "#/definitions/autoware_image_transport_decompressor"
        }
      },
      "required": ["ros__parameters"]
    }
  },
  "required": ["/**"]
}
